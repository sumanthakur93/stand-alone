export type AuthStateType = {
    isAuth: boolean;
    setIsAuth: (isAuth: boolean) => void;
}

export type RefreshStateType = {
    refreshState: boolean;
    setRefreshState: (isAuth: boolean) => void;
}